import React from 'react';

function Profile() {
    return (
        <div className="container" style={{ textAlign: 'center', padding: '40px' }}>
            <h1>User Profile</h1>
            <p>User profiles are currently disabled as we rebuild the authentication system.</p>
        </div>
    );
}

export default Profile;
